---
paths: ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", "**/*.mjs", "**/*.cjs"]
---

Editing TypeScript / JavaScript - load `typescript` before the edit; conventions are the source of
truth, not recall. The framework-agnostic baseline for any `.ts`/`.tsx`/`.js`/`.jsx`/`.mjs`/`.cjs`
file; framework layers (Angular / Ionic) ship as their own rules and stack on top where installed.
Skip one-line tweaks.
